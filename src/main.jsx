import React, { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import App16 from "./practice/App16.jsx";

createRoot(document.getElementById("root")).render(
  <StrictMode>
    <App16 />
  </StrictMode>,
);
