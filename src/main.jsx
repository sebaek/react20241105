import React, { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import MyApp22 from "./apps/MyApp22.jsx";

createRoot(document.getElementById("root")).render(
  <StrictMode>
    <MyApp22 />
  </StrictMode>,
);
